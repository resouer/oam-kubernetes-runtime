apiVersion: core.oam.dev/v1alpha2
kind: Component
metadata:
  name: web-service-component
spec:
  workload:
    type: web-service
    spec:
      containers:
        - name: web
          image: nginx
          ports:
            - containerPort: 80
              name: web
          env:
            - name: TEST_ENV
              value: test
  parameters:
    - name: image
      fieldPaths:
        - spec.containers[0].image